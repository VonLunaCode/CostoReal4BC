import 'package:flutter/material.dart';

class KitchyColors {
  // Fondos
  static const Color background     = Color(0xFFF5F0E8); 
  static const Color surface        = Color(0xFFFFFFFF); 
  static const Color surfaceWarm    = Color(0xFFFAF6EF); 

  // Primario (dorado/ámbar)
  static const Color primary        = Color(0xFFC8913A); 
  static const Color primaryLight   = Color(0xFFE8C96B); 
  static const Color primaryDark    = Color(0xFF8B7355); 

  // Texto
  static const Color textPrimary    = Color(0xFF2C1F0E); 
  static const Color textSecondary  = Color(0xFF8B7355); 
  static const Color textDisabled   = Color(0xFFB0A590); 

  // UI / Bordes
  static const Color border         = Color(0xFFDDD5C4); 
  static const Color divider        = Color(0xFFEEE8DC); 
  static const Color chipUnselected = Color(0xFFEEE8DC); 

  // Acento / Estado
  static const Color success        = Color(0xFF2E7D32); 
  static const Color iconMuted      = Color(0xFFB0A590); 

  // Bottom Nav
  static const Color navBackground  = Color(0xFFFFFFFF); 
  static const Color navActive      = Color(0xFFC8913A); 
  static const Color navInactive    = Color(0xFFB0A590); 

  // Estado del pedido — pasos
  static const Color stepActive     = Color(0xFFC8913A); 
  static const Color stepCompleted  = Color(0xFF2E7D32); 
  static const Color stepInactive   = Color(0xFFDDD5C4); 

  // WhatsApp button
  static const Color whatsapp       = Color(0xFF25D366); 
  static const Color whatsappText   = Color(0xFFFFFFFF); 

  // Totales
  static const Color totalLabel     = Color(0xFF8B7355); 
  static const Color totalAmount    = Color(0xFF2C1F0E); 

  // Info cards (dirección / notas)
  static const Color infoCardBg     = Color(0xFFF5F0E8); 
}
